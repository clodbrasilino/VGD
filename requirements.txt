# VGD — Visually Guided Decoding (ICLR 2025)
# Clean requirements for pip-based environments (Colab, venv, conda without local file paths)
#
# NOTE: torch / torchvision are intentionally NOT pinned here.
#   - Colab / Kaggle already ship a CUDA build of PyTorch.
#   - If you need a specific CUDA version install it separately:
#       pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
#
# NOTE: nvidia-* CUDA wheels are also excluded.
#   They are only needed when installing torch via pip on a bare Linux machine.
#   Most GPU-enabled runtimes (Colab, RunPod, Lambda) ship them via the system CUDA toolkit.

# ── Core ML / Diffusion ──────────────────────────────────────────────────────
accelerate>=0.26.0
diffusers>=0.27.0
transformers>=4.41.0
tokenizers>=0.19.0
safetensors>=0.4.0
bitsandbytes>=0.41.0
compel>=2.0.0

# ── CLIP ─────────────────────────────────────────────────────────────────────
open-clip-torch>=2.24.0
sentence-transformers>=2.7.0

# ── Data / HuggingFace ecosystem ─────────────────────────────────────────────
datasets>=2.18.0
evaluate>=0.4.0
huggingface-hub>=0.23.0

# ── Scientific computing ─────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0

# ── Image processing ─────────────────────────────────────────────────────────
pillow>=10.0.0
opencv-python>=4.8.0
timm>=0.9.0

# ── NLP utilities ────────────────────────────────────────────────────────────
ftfy>=6.1.0
regex>=2023.0.0
sentencepiece>=0.1.99
bert-score>=0.3.13

# ── Config / CLI ─────────────────────────────────────────────────────────────
omegaconf>=2.3.0
PyYAML>=6.0.0
click>=8.1.0

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.16.0
GitPython>=3.1.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.65.0
matplotlib>=3.7.0
networkx>=3.0
lmdb>=1.4.0
gdown>=4.7.0
psutil>=5.9.0
packaging>=23.0
